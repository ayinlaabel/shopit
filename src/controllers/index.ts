/**
 * This is the place to create global controllers to be used in-app.
 * each controller must be in a folder and each folder must define the following:
 * - the controller itself (e.g. index.tsx)
 * - the local interface used by the controller (e.g. interface.ts)
 */