/**
 * This is the place to create global components to be used in-app.
 * each component must be in a folder and each folder must define the following:
 * - the component itself (e.g. index.tsx)
 * - the local interface used by the component (e.g. interface.ts)
 */
